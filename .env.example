# CoinDCX (reused from TradeVerse)
COINDCX_API_KEY=
COINDCX_API_SECRET=

# Telegram (NEW bot, separate from TradeVerse's)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Optional tuning (defaults shown)
SL_BUFFER_PCT=0.5
POLL_INTERVAL_SECONDS=30

# Persistent volume mount path on Railway (attach a volume at this path)
STATE_DIR=/data
